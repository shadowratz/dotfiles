{ config, pkgs, ... }:

{
  # macOS-specific home-manager configuration goes here.
  # Example: add macOS-only packages, settings, or environment variables.

  # home.packages = [
  #   pkgs.iterm2
  #   pkgs.rectangle
  # ];

  # You can also set macOS-specific options here.
}
