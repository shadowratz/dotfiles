{ config, pkgs, ... }:

{
  # Hyprland home-manager module stub
  # Migrate your Hyprland configuration here using home.file or home-manager options.
  # Example:
  # home.file.".config/hypr/hyprland.conf".source = ./path/to/your/hyprland.conf;

  # Add Hyprland to your user packages (if desired)
  # home.packages = [ pkgs.hyprland ];
}
