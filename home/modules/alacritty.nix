{ config, lib, pkgs, ... }:

{
  # Alacritty Home Manager module
  # This module manages the Alacritty terminal emulator configuration for your user.
  # Use home.file to symlink your Alacritty config from your dotfiles repo to ~/.config/alacritty/alacritty.toml.
  # Example (uncomment and adjust as needed):
  # home.file.".config/alacritty/alacritty.toml".source = ../../alacritty/.config/alacritty/alacritty.toml;
}


